# Changelog

Формат: [Keep a Changelog](https://keepachangelog.com/ru/1.1.0/), версии — [SemVer](https://semver.org/lang/ru/).

## [Unreleased]

### Добавлено

- **Видно, кто именно работает.** Лента подписывает строки агентом: значок `◆` открывает ветку с именем агента, его моделью и ролью (`ORCHESTRATOR` для сессии, в которую печатает пользователь, `EXECUTOR` для субагента, посаженного на локальный маршрут исполнителя, `SUBAGENT` для остальных). Имя субагента берётся из его дескриптора, из проекции `subagent` или — у одноразового исполнителя, который дескриптора не пишет, — из самого вызова `executor`. Данные берутся только из фактов хоста: списка сессий (`parentSessionId`, `origin`, `agentPreset`), событий `request/header` и `subagent/descriptor` в логе ребёнка и проекций `subagent`/`subagentActivity`; роль не выдумывается — при выключенном оркестраторе у корня её нет. Подписи отключаются `KIBBORG_NO_AGENTS=1`.

### Добавлено

- **Журнал действий для отладки.** Kibborg ведёт JSONL-журнал того, что делает пользователь и что из этого вышло: каждая клавиша с указанием, кто её обработал (`menu`, `dialog`, `reader`, `surface`, `loop`), каждое действие с временем выполнения, результатом и причиной отказа, каждая команда и каждый ход с расходом, а также всё, что хост и зависимости пишут в `stderr` (раньше стек-трейс мелькал в кадре и исчезал). Записи вида `scope: reader, action: toggle-entry, ok: false, reason: "у записи нет скрытых строк: разворачивать нечего"` отвечают на вопрос «нажал — и ничего», не воспроизводя сессию заново. Уровни: `info` по умолчанию, `trace` добавляет каждую клавишу и каждый кадр с временем и объёмом записи, `off` выключает. Файл — `$DSH_HOME/logs/kibborg.jsonl` (переопределяется `KIBBORG_LOG_FILE`), ротация по 5 МБ с пятью копиями; команда `/logs` печатает последние записи прямо в ленту, пропуская кадры. Измерено, что журнал не мешает работе: шаг стрелки на ленте в 20 000 записей стоит 3.33 мс против 2.89 мс без журнала.

### Изменено

- **Режим разрешений переключается клавишей и берётся у хоста.** `Shift+Tab` переводит на следующий пресет, а `/permission` открывает список того, что действительно смонтировано: раньше оба предлагали выдуманные `Ask`, `Plan`, `Agent` и `YOLO`, и хост отвечал `unknown preset "YOLO"`. Теперь имена приходят из сервиса разрешений (`read-only`, `workspace-write`, `danger-full-access` в профиле `kibborg`), а бейдж в нижней границе поля показывает то, что применено.
- **`Tab` переводит фокус, `Ctrl+X` печатает справку.** На пустом вводе `Tab` берёт ленту (та же метка `▌`, что и у стрелок), второе нажатие возвращает ввод; с черновиком `Tab` по-прежнему дополняет. `Ctrl+X` печатает список клавиш в ленту, а не в модальное окно: его можно читать, прокручивать и копировать целиком.
- **Тема `terminal`.** Третья тема поверхности: Kibborg не красит текст вовсе, оставляя жирность и приглушение, поэтому цвета берутся из профиля терминала — темированного эмулятора или привычной схемы пользователя. `ice` остаётся палитрой по умолчанию, `mono` печатает вообще без атрибутов.
- **Видно, что модель думала.** Рассуждения по-прежнему не показываются, но в ленте появляется строка `♦ Думал 2.2s`: пауза, которую пользователь иначе принимал бы за зависание, получает объяснение и длительность. Время считается от первого куска рассуждений до первого куска ответа.
- **Diff читается по изменениям.** В блоке правки неизменённый контекст приглушён, а добавленные и удалённые строки несут по одному акценту (`+` зелёным, `−` красным), поэтому взгляд падает на само изменение, а не на строки вокруг него.
- **Лента читается с клавиатуры.** На пустом вводе `↑` и `↓` водят по записям ленты, выбранная помечается слева знаком `▌` (ответ, вызов инструмента, задача), `Enter` разворачивает её, `h`/`l` сворачивают и разворачивают, `y` копирует содержимое целиком — вместе с аргументами и выводом инструмента. Легенда под полем ввода меняется на `Enter — развернуть · h/l — свернуть/развернуть · y — копировать · ↑↓ — выбор`, а прокрутка ленты остаётся на `PgUp`/`PgDn` и колесе. Это возвращает работу с блоками без мыши: Kibborg, как и Grok CLI, Codex, Claude Code и OpenCode, мышь не перехватывает, поэтому клик по строке «▸ аргументы · вывод» в обычном терминале недоступен.
- **Скроллбар справа от ленты.** Пока лента длиннее окна, в последней колонке стоит тонкая полоса: трек `│` и бегунок `█`, который уезжает вниз, когда вид следует за хвостом, и поднимается, когда читатель прокручивает назад. Лента, помещающаяся в окно, полосы не показывает.
- **Строка работы в ленте.** Пока идёт ход, последняя строка ленты читается как `♦ Запускает go test ./...  24.0s  ↓34.7k  [stop]`: что агент делает сейчас, сколько уже работает, сколько токенов потратил и какой клавишей это остановить. Строка обновляется четыре раза в секунду по событиям хоста (имя инструмента из вызова, расход из usage), а когда приходит ответ — убирается: в ленте остаётся результат, а не отчёт о процессе.
- **Лента стала перепиской со временем.** Задача пользователя — это его собственная строка `> текст` с временем у правого края, без отдельной подписи «You»: маркер уже говорит, кто написал. Ответ ассистента несёт время первой читаемой строки, потому что минуты — часть разговора, а не только служебная подпись. Время ставится в момент появления сообщения и не меняется при перерисовке; отключается настройкой `kibborg-cli.timestamps` (по умолчанию включена, как в Grok CLI, Codex и Claude Code).
- **Верхняя строка отвечает «где я».** Шапка перестала печатать бренд и версию и стала строкой места: слева `≡ <ветка><*>` и рабочий каталог, справа занятая доля контекста (`ctx 18%`) и имя открытой панели. Терминал и так подписан снаружи, а место и контекст нужны в каждой строке работы.
- **Поле ввода — рамка, а статус вписан в её нижнюю границу.** Композер стал боксом: `╭─…─╮`, строки черновика с промптом `> `, строка подсказок клавиш и нижняя граница `╰─ kibborg/… · Agent ─────────── 3 агента · 7 задач ─╯`. Отдельная статусная строка внизу экрана убрана: модель, режим и счётчики теперь там, где пользователь смотрит при наборе, и не стоят лишней строки. Сборку фактов для борта делает общая функция `composerFacts`, поэтому все три поверхности (полноэкранная, зона inline, `--fullscreen` клиента) показывают одно и то же.
- **Строка подсказок клавиш зависит от контекста.** Во время хода в ней стоит `Ctrl+C — прервать ход   ↑↓ — читать ленту`, при наборе `/` или `@` — подсказка автодополнения, в покое — общий список (`@ файлы   / команды   ! shell   shift+enter — новая строка`).
- **Лента отвечает на «что происходит», а не показывает протокол.** Строка инструмента печатает человеческое действие («Запускает go test ./...», «Правит engine-go/guard.go», «Делегирует: посчитать строки»), а аргументы, вывод и diff ушли во вторичный слой — строку `▸ аргументы · вывод`, которая раскрывается клавишей Enter на выбранной записи. Ответ модели всегда развёрнут и рендерится как документ: заголовки с маркером и линией под H1, отбивка пустой строкой перед каждым блоком, таблицы с линейкой под шапкой и под телом, код в рамке `┌─ lang … └─`, списки с разными маркерами уровней, кликабельные ссылки и пути. Предыдущие ответы по-прежнему сворачиваются.
- **Цвет отвечает за состояние, а не за автора.** Палитра переведена на GrokNight: нейтральная база, приглушённые границы и один акцент на внимание (промпт, выбранная строка, ссылка). Агенты больше не получают по своему цвету — имя, модель и роль читаются словами, а шесть оттенков делали ленту похожей на диаграмму и менялись от сборки к сборке. Токены действий (`ActionThink`, `ActionTool`, `ActionFile`, `ActionDelegate`) тоже приглушены: что делает строка, говорят её глиф и слова, а громким остаётся результат — `Success`, `Warn`, `Error`, `DiffAdd`, `DiffRemove`.
- **Субагент — бокс, а не строка.** Делегирование рисуется рамкой `┌─ ◐ имя · модель · роль ─── ◐ WORKING ┐`, строки агента идут между стенками, закрытие — `└─ ✓ имя ─── ✔ DONE ┘`. Завершённая ветка сворачивается в одну строку `⋯ N шагов этой ветки — Enter, чтобы развернуть`; `Enter` на строке ветки разворачивает и сворачивает её.
- **Клик и выделение разведены.** При включённом перехвате мыши (`KIBBORG_MOUSE=1`) клик (нажатие и отпускание в одной клетке) раскрывает блок и ничего не копирует, а протяжка копирует текст; по умолчанию мышь принадлежит терминалу. Исправлено смещение строк, из-за которого клик по строке «ещё N строк» уходил в соседнюю строку и копировал её вместо раскрытия — карту кликов теперь строит сама область ленты.
- **Шапка из двух строк и живой статус.** Шапка больше не тратит строку на декоративную волну `▓▒░`: одна строка с брендом, версией, каталогом и моделью плюс правило. Статус во время хода читается как `✦ Working 26.6s · ctx 26% ▓▓▓░░░░░░░ · 3 агента · 7 задач · Agent · Esc прерывает ход`, счётчики агентов и задач обновляются по ходу, а после завершения сбрасываются.
- **Кадр больше не пересобирает ленту целиком.** Транскрипт рендерится частями и переиспользуется, пока модель ленты не изменилась, а окно прокрутки берёт только видимые строки. На ленте в 3000 записей (16 000 строк) кадр без изменений подешевел с ~355 мс до ~0,13 мс, кадр во время потока — с ~339 мс до ~3 мс; кэш отрендеренных записей держит сессию целиком, а поток текста ответа больше не ищет свою запись по всей ленте.
- **`+N` и `−M` печатаются двумя цветами** (`+N` зелёным, `−M` красным) и в ленте, и в scrollback-режиме.
- **Строка инструмента обрезается по колонкам, а не по символам.** Длинный путь или команда больше не вылезает за ширину терминала и не теряет самое нужное: заголовок режется ровно на столько знаков, сколько осталось после глифа и хвоста (`+N`, `−M`, время, `▸ аргументы · вывод`), поэтому у пути остаётся начало и **имя файла с расширением** в конце (`Читает C:/Users/lex66/…/dist/index.js`), а у команды — её начало. Тот же приём применён к строке инструмента в истории `/resume`, где аргумент обрезался жёстко по 80-му символу и терял имя файла.

### Исправлено

- **Вставленный текст больше не превращается в мусор терминала.** Windows-терминал в режиме win32-input-mode присылает по одному отчёту на каждую клавишу, а вставка — это их пачка; когда граница чтения попадала между `ESC` и остальным отчётом, префикс уходил как отдельный `Escape`, а хвост вроде `[13;28;13;1;0;1_` печатался в поле ввода. Теперь такие хвосты распознаются по форме, отбрасываются и записываются в журнал (`scope: input, action: noise`), а символ вставки из отчёта восстанавливается. Текст, который лишь начинается с `[` (`[0;30m`, `[1;2]`), доходит до поля ввода как есть.
- **Прокрутка ленты больше не тормозит на длинной сессии.** Перемещение выбора пересобирало весь отрендеренный транскрипт: метка `▌` жила внутри рендера записи, поэтому выбранная запись входила в ключ кэша и меняла его целиком. На сессии в 20 000 записей один шаг стоил 45 мс, на 10 000 — 20 мс, то есть колесо и стрелки упирались в перерисовку всей ленты. Метка переехала в слой отрисовки: строки знают свою запись, а вид помечает выбранную, поэтому шаг теперь стоит 1–3 мс независимо от размера ленты (замеры в `tests/perf-scroll.mjs`).
- **Палитра и стартовый экран больше не обещают несуществующих клавиш.** Из списка команд убраны подписи `ctrl+n`, `ctrl+q`, `ctrl+m`, `f3` и `s-tab`: поверхность их не обрабатывает, а `Ctrl+M` в терминале и вовсе приходит как обычный Enter, поэтому подпись читалась как сломанная клавиша. Раскладка палитры отдала освободившуюся колонку описанию команды, поле подсказки исчезло из типа записи. Стартовый экран вместо них показывает то, что действительно работает: `[enter]`, `[/]`, `[tab]`, `[shift+tab]`, `[ctrl+x]`; подсказка про `Ctrl+Q` заменена на `Ctrl+D`, а описание `Shift+Tab` — на фактические пресеты хоста.
- **Строка «↩ назад» действительно возвращает в список команд.** После выбора пункта во вложенном списке (модель, сессия, режим) палитра оставалась на экране — с прежним содержимым и отфильтрованная набранной строкой, поэтому вместо списка команд показывалось «нет совпадений», а сама строка «назад» выглядела неработающей. Теперь любой выбор закрывает список, а возврат на уровень выше открывает палитру команд с пустым фильтром; поверхность не затирает черновик и выделение, которые этот возврат установил.
- **`/new` действительно начинает новую сессию.** Команда, набранная во время хода, выполнялась как steering-сообщение: `/new` уходил модели, сессия не менялась, лента и счётчик контекста оставались прежними. Теперь команда поверхности обрабатывается как команда в любой момент: текущий ход отменяется, поверхность дожидается его последних строк, затем очищает ленту, сбрасывает `ctx`, токены, время и счётчики агентов и задач. Измерения отменённого хода больше не могут переписать статус новой сессии — ход помнит номер поколения сессии.
- **`Esc` больше не отменяет ход.** Случайный `Esc` обрывал ответ; теперь ход снимает только `Ctrl+C`, а `Esc` во время работы ничего не отменяет и говорит, чем прервать. Так же поступает Grok CLI.
- **Каретка стоит в поле ввода, а не строкой выше.** Курсор адресовался на строку вверх от последней строки черновика: в однострочном вводе он попадал на строку подсказки, а при `Shift+Enter` — на предыдущую строку текста. Теперь строка каретки считается от той же границы бокса, что и рисование, а маркер прокрутки `▲ +N` учитывается в колонке только тогда, когда делит строку с кареткой.
- **Выход из CLI.** В палитре `Ctrl`/`/` появились команды `SESSION`: `/exit` и `/quit` (их не было в списке, потому что палитра строилась только по каталогу хоста). `Ctrl+C` вне хода теперь завершает работу, а не требует второго нажатия: первый нажатие очищает недописанный черновик, повторное — выходит.
- **Копирование выделением мышью.** Нажатие снова сразу начинает выделение и подсвечивает область: раньше нажатие лишь «взводило» клик по строке-маркеру, и в терминале, который не присылает промежуточные движения, выделения не появлялось вовсе. Клик без протяжки по-прежнему раскрывает блок и ничего не копирует, протяжка копирует текст.
- **Shift+Enter и рост окна ввода.** Курсор встаёт на новую строку: раньше он адресовался на строку ниже поля ввода и попадал на строку подсказки. Окно ввода растёт вместе с черновиком до восьми строк, а дальше включает собственную прокрутку и показывает `▲ N строк выше`, чтобы было видно, что черновик длиннее окна.

### Исправлено

- **Модель снова может спрашивать и предлагать варианты.** Инструмент `ask_user_question` не был смонтирован в профиле `kibborg` — базовый слой даёт только шов `userQuestions`, а сам инструмент живёт отдельным плагином, поэтому модель физически не могла задать вопрос. Ряд добавлен в `@kibborg/cli-bundle`. Окно вопроса теперь выбирается стрелками (`↑`/`↓`), `Space` отмечает варианты в множественном выборе, `Enter` подтверждает выделенный вариант, `Esc` отменяет; ввод номера по-прежнему работает как альтернатива. Раньше окно показывалось, но стрелки двигали только внутреннюю подсветку рамки, а `Enter` требовал ввести номер и отвечал «that was not an option number».
- **Окно ввода растёт до восьми строк в любом режиме.** Полноэкранная поверхность рисовала черновик одной строкой с обрезкой, а `zoneLines` (композерный режим) — четырьмя фиксированными строками, поэтому `Shift+Enter` не давал видимой новой строки. Теперь общий рендер композера показывает до восьми строк, дальше прокручивает собственный текст и пишет `▲ +N`, а каретка встаёт на новую строку под текстом (раньше она адресовалась строкой ниже окна и попадала на строку подсказки).
- **Подтверждения работают в полноэкранном режиме.** `runFullscreen` не передавал `onApproval`/`onQuestion`, поэтому хост применял headless-политику: подтверждения молча отклонялись, а вопросы не доходили до пользователя. Оба колбэка добавлены вместе с отрисовкой рамки запроса и обработкой клавиш (`y`/`a`/`n`, стрелки, `Enter`, `Esc`).
- **Диагностика хоста больше не рисуется поверх кадра.** Стек-трейс вроде `api-proxy: stream error: ERR_INVALID_STATE` печатался прямо в область ввода, ломая кадр; теперь строки stderr во время работы поверхности складываются в ленту. Отмена хода сначала просит хост остановить турн и только через две секунды обрывает поток — это и было источником того стек-трейса.
- **Быстрый старт: 19 секунд → 8.** Две причины: `sessionPersistence.list()` читал заголовки 1462 сессий последовательно, по одному обращению к диску за раз (теперь чтение идёт с ограниченным параллелизмом), а поверхность при открытии сканировала рабочую директорию и список сессий для автодополнения (теперь этот скан отложен до первого `Tab`, о чём строка «собираю подсказки…»).

## [0.1.0] — 2025-09-12

Первая публичная версия CLI.

### Добавлено

- **Команда `kibborg` для любой оболочки**: `install.ps1` и `install.sh` создают шим, который запускает собранный `apps/cli/lib/bin.js`; PATH правится в пользовательской области, администратор не нужен.
- **Три режима работы**: интерактивный TUI (inline и `--fullscreen`), headless (`-p`, `--output-format text|json|stream-json`) и сетевой (`serve` на одной машине, `attach` к нему, `discover` через mDNS).
- **Палитра команд с подменю**: `/` открывает список, `/model`, `/effort`, `/permission`, `/resume` открывают выбор, строка `↩ назад` и `Escape` возвращают на уровень выше.
- **Лента инструментов целиком**: `IN` с полным JSON аргументов, `OUT` с полным ответом, diff со знаками `+`/`−` и сводкой `+N −M`, время вызова.
- **Markdown в ответах**: заголовки, списки, таблицы с выравниванием, блоки кода с рельсом, инлайн-код, ссылки.
- **Кликабельные ссылки и пути**: OSC 8 гиперссылки, `Ctrl`+клик открывает файл или URL.
- **Выделение и копирование**: протяжка мышью подсвечивает область и кладёт текст в буфер обмена в правильной кодировке (UTF-8 через `Set-Clipboard` на Windows).
- **Сворачивание длинных блоков**: записи длиннее пяти строк (`COLLAPSED_LINES`) показываются началом и строкой «⋯ ещё N строк — Enter, чтобы развернуть».
- **Живой интерфейс**: спиннер с меняющимся цветом, статусная строка, где модель, `ctx` и режим остаются видимыми во время хода, подтверждения действий исчезают через пять секунд.
- **Навигация**: стрелки прокручивают ленту на пустом вводе и листают историю на непустом; `Shift+Enter` переносит строку (Win32 input mode и kitty keyboard protocol), `Ctrl+J` — перенос в любом терминале.
- **Настройки и реестры**: секция `kibborg-cli` (тема, метки времени, многострочный ввод, режим экрана), `settings`, `auth`, `models`, `mcp`, `skills`, `tools`, `agents`, `jobs`, `commands`, `stats`, `doctor`, `export`.
- **Развёртывание**: `deploy/kibborg.service` (systemd), `deploy/Dockerfile`, `deploy/docker-compose.yml`, `deploy/healthcheck.sh`, примеры для GitHub Actions и cron.
- **Проверка качества**: 235 юнит-тестов, `oxlint` без замечаний, PTY-сценарии меню, команд и полноэкранного режима, инструмент кадров `tests/frame-render.mjs`.

### Безопасность

- `serve` отказывается стартовать на не-loopback интерфейсе без токена и печатает причину с кодом выхода 2.
- Гейт `/api` проверяет `Origin` и токен; без токена — 401.
- `--safe` применяет режим `Ask` к сессии даже если в настройках сохранён более широкий пресет.
