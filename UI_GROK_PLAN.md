# План: интерфейс Kibborg CLI по образцу Grok CLI

План зафиксирован по живым фактам: установленный Grok CLI 1.0.30 (документация `~/.grok/docs/user-guide/*.md` и кадры в PTY), а также поведение установленных Codex, Claude Code и OpenCode. Инструменты съёмки кадров — `Kibborg_CLI/tests/ref-probe.mjs` (сырой PTY-вывод и терминальные режимы) и `Kibborg_CLI/tests/ref-render.mjs` (кадр из потока).

## Основание: что делают четыре CLI

| CLI | Перехват мыши | Альтернативный экран | Bracketed paste | Sync output |
|---|---|---|---|---|
| Grok | **нет** | нет | да | да |
| Codex | **нет** | нет | да | да |
| Claude Code | **нет** | нет | да | нет |
| OpenCode | **нет** | нет | да | да (+ truecolor, DEC 2027) |

Вывод: ни один не забирает мышь, поэтому выделение, копирование и колесо у них делает терминал. Kibborg включал `1000h/1002h/1006h` и повторял эти жесты сам — отсюда сломанный скролл, выделение и копирование.

Модель Grok, которая берётся за образец:

```
  main D:\...\Kibborg_CLI                    34K / 500K │ [Dashboard]
     > Выполни в терминале команду echo привет …            5:32 PM
     ♦ Run Print greeting to terminal
     ♦ Thought for 2.2s
     Команда выполнена. …                                    5:32 PM
     ♦ Ask Продолжить или остановиться?
    ♦ Waiting on answers for …            24s ↓34.7k [stop]
  │  1 (○) Продолжить    Продолжить работу
  │  2 (○) Остановиться  Остановить работу
  │  z (○) Type your answer here
  │  ↑/↓ navigate · y copy                        Enter:submit
  ╭────────────────────────────────────────────────────────────────╮
  │ >                                                               │
  ╰──── Grok 4.6 (medium) · always-approve ─────────────────────────╯
```

---

## Этап 0 — аварийные исправления

| ID | Статус | Задача | Артефакт | Проверка |
|---|---|---|---|---|
| 0.1 | **done** | Мышь не перехватывается: выделение, копирование и колесо — терминальные (`KIBBORG_MOUSE=1` включает обратно) | `packages/tui/src/screen.ts` | зонд PTY: mouse-последовательности отсутствуют; тесты `core.spec.ts`, `surface.spec.ts` |
| 0.2 | **done** | Убран стек-трейс `api-proxy: stream error: ERR_INVALID_STATE` при старте: отмена подписки потребителем не считается сбоем | `packages/host/apiproxy/src/fetch/handler.ts` | `kibborg tools` без вывода ошибок |
| 0.3 | **done** | Карточка вопроса: `1 (○) Название Описание`, строка `z` для своего ответа, `↑↓`/`Tab`/цифры/`Enter`/`Esc`, отправка набранного текста | `packages/tui/src/menus.ts`, `packages/client-node/src/repl.ts`, `fullscreen.ts` | `question.spec.ts`; живой прогон: карточка «Вопрос 1/1», свободный ответ уходит |

---

## Этап 1 — каркас экрана

| ID | Статус | Задача | Артефакт | Проверка |
|---|---|---|---|---|
| 1.1 | **done** | Верхняя строка: `≡ main <cwd>` слева; справа занятый контекст (`ctx 18%`) и имя панели | `packages/tui/src/header.ts` | кадр 114 колонок; `surface.spec.ts` |
| 1.2 | **done** | Лента без рамок: сообщение пользователя `> текст` со временем справа; ответ ассистента с отступом и временем | `packages/tui/src/log.ts` | `render.spec.ts`, `surface.spec.ts`; живой кадр |
| 1.3 | **done** | Строка работы `♦ <действие>  <время>  ↓токены  [stop]`: живёт в ленте, пока идёт ход, и заменяется ответом | `packages/tui/src/log.ts`, `packages/client-node/src/repl.ts` | `surface.spec.ts`; живой кадр во время хода |
| 1.4 | **done** | Поле ввода с рамкой; статус (`модель · режим`) вписан в нижнюю границу, счётчики — у правого угла | `packages/tui/src/composer.ts`, `packages/tui/src/app.ts` | `render.spec.ts`, `input.spec.ts`; живой кадр |
| 1.5 | **done** | Контекстная строка подсказок клавиш внутри рамки: легенда поверхностей, подсказка автодополнения, `Ctrl+C — прервать ход` во время хода | `packages/tui/src/composer.ts`, `packages/tui/src/app.ts` | `render.spec.ts`; живой кадр во время хода |
| 1.6 | **done** | Тонкий скроллбар справа: одна колонка, thumb `█` на треке `│`, только когда лента длиннее окна | `packages/tui/src/logview.ts` | `surface.spec.ts`; живой кадр |

---

## Этап 2 — отображение работы агента

| ID | Статус | Задача | Артефакт | Проверка |
|---|---|---|---|---|
| 2.1 | **done** | Вызовы инструментов как человеческое действие («Запускает …», «Читает …», «Ищет …», «Правит …», «Делегирует: …») вместо сырых имён и JSON | `packages/client-node/src/arguments.ts`, `packages/tui/src/log.ts` | `tool-title.spec.ts` + кадр |
| 2.2 | **done** | Результат инструмента сворачивается под вызовом, `Enter` — просмотр целиком (мышь не перехватывается, поэтому раскрытие клавиатурное) | `packages/tui/src/log.ts`, `packages/tui/src/app.ts` | `surface.spec.ts`; живой кадр |
| 2.3 | **done** | Diff в стиле Grok: приглушённый контекст, один акцент на добавление и удаление | `packages/tui/src/log.ts` | `surface.spec.ts` (токены `DiffAdd`/`DiffRemove`, контекст приглушён) |
| 2.4 | **done** | Размышления отдельными блоками с длительностью: `♦ Думал 2.2s`, текст рассуждений не показывается | `packages/tui/src/render.ts`, `packages/tui/src/log-renderer.ts`, `packages/client-node/src/turn.ts` | `surface.spec.ts` |
| 2.5 | **done** | Субагенты как дочерние сессии: бокс с именем, моделью, ролью и состоянием, сводка родителю при закрытии | `packages/tui/src/log.ts`, `packages/client-node/src/agents.ts` | `agents.spec.ts` |

---

## Этап 3 — цвета и роли

| ID | Статус | Задача | Артефакт | Проверка |
|---|---|---|---|---|
| 3.1 | **done** | Палитра GrokNight: нейтральная база, приглушённые границы и скроллбар, один акцент | `packages/tui/src/tokens.ts` | `core.spec.ts` (один акцент, границы приглушены); кадр |
| 3.2 | **done** | Убран цвет на каждого агента: имя — обычным текстом, роль — приглушённо, цвет только на семантику (успех/ошибка/diff), токены действий приглушены | `packages/tui/src/tokens.ts`, `log.ts` | `agents.spec.ts`, `core.spec.ts` |
| 3.3 | **done** | Тема `terminal` (без цветов, только атрибуты; цвета из профиля терминала) | `packages/tui/src/tokens.ts`, `packages/client-node/src/surface-settings.ts`, `packages/cli-bundle/src/index.ts` | `core.spec.ts`; README |

---

## Этап 4 — клавиши и поведение

| ID | Статус | Задача | Артефакт | Проверка |
|---|---|---|---|---|
| 4.1 | **done** | `Tab` — фокус между лентой и вводом; `↑↓` — выбор записи; `h`/`l` — свернуть/развернуть; `y` — копировать блок | `packages/tui/src/app.ts`, `packages/client-node/src/repl.ts` | `surface.spec.ts`; живой кадр |
| 4.2 | **done** | `Ctrl+C` отменяет ход; `Esc` — нет: читает ленту и говорит, чем прервать | `packages/client-node/src/repl.ts` | живой кадр во время хода |
| 4.3 | **done** | `Shift+Tab` — смена режима разрешений; список пресетов берётся из сервиса хоста, режим виден в нижней границе поля | `packages/client-node/src/repl.ts`, `panel.ts`, `index.ts` | живой кадр: `режим: read-only`, бейдж `read-only` |
| 4.4 | **done** | `Ctrl+x` — справка по клавишам, печатается в ленту | `packages/tui/src/input.ts`, `packages/client-node/src/repl.ts` | `input.spec.ts`; живой кадр |

---

## Этап 5 — проверка и документация

| ID | Статус | Задача | Артефакт | Проверка |
|---|---|---|---|---|
| 5.1 | **done** | Юнит-тесты новых компонентов (лента, рамка, палитра, клавиши, строка работы, размышления) | `packages/tui/tests/*`, `packages/client-node/tests/*` | `pnpm run test` — 286 тестов |
| 5.2 | **done** | PTY-кадры на каждый этап: `pty-frame`, `pty-menu`, `pty-commands`, `pty-fullscreen` | `Kibborg_CLI/tests/*` | все четыре сценария проходят |
| 5.3 | **done** | README, CHANGELOG, TASKS.md по факту изменений | `Kibborg_CLI/*.md` | `pnpm run lint`, ревью Grok CLI |

---

## Известный остаток

Основная поверхность (`kibborg` без флагов, `createApp`) получила всё перечисленное. Два второстепенных режима отстают, и это зафиксировано, а не забыто:

1. `--fullscreen` (`packages/client-node/src/fullscreen.ts`) рисует свою ленту строками: строка места и рамка ввода там есть, выбора записи, строки работы и блока размышлений нет.
2. `KIBBORG_INLINE=1` (`packages/tui/src/zone.ts`) рисует ту же рамку, но без шапки: доля контекста и ветка переехали в счётчики нижней границы, выбора записи нет.
3. `/resume` печатает историю заметками (`emit` → `kind: 'info'`), а не записями ленты: у восстановленной истории нет времени сообщений, сворачивания и метки выбора.
4. Рамку ввода рисуют две функции: `drawComposer` (буфер основной поверхности) и `composerFrame` (строки для `--fullscreen` и зоны). Геометрия сведена вручную и совпадает, но остаётся двумя реализациями.

Все четыре пункта — известное незакрытое, а не забытое: основная поверхность (`kibborg` без флагов) получила всё перечисленное в этапах 1–4.

---

## Порядок и правила

1. Этапы идут по номерам: каждый следующий опирается на предыдущий.
2. Внутри этапа сначала меняется рендер (tui), затем поведение (client-node), затем тесты.
3. Этап закрыт, когда: тесты зелёные, `oxlint` чистый, есть живой кадр, README/CHANGELOG обновлены.
4. Ничего из `vendor/` и ядра не меняется без отдельной причины, зафиксированной в описании коммита.
